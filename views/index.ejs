<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD dengan MySQL, Node.js, EJS, dan Express</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>Data</h1>
        <form action="/tambah" method="post">
            <div class="mb-3">
                <input type="text" name="nama" class="form-control" placeholder="Nama">
            </div>
            <div class="mb-3">
                <input type="text" name="alamat" class="form-control" placeholder="Alamat">
            </div>
            <button type="submit" class="btn btn-primary">Tambah</button>
        </form>
        <hr>
        <h2>Daftar Data</h2>
        <ul class="list-group">
            <% for(var i=0; i < result.length; i++) { %>
                <li class="list-group-item">
                    <%= result[i].nama %> - <%= result[i].alamat %>
                    <form action="/edit" method="post" style="display:inline;">
                        <input type="hidden" name="id" value="<%= result[i].id %>">
                        <input type="text" name="nama" class="form-control" value="<%= result[i].nama %>">
                        <input type="text" name="alamat" class="form-control" value="<%= result[i].alamat %>">
                        <button type="submit" class="btn btn-primary">Edit</button>
                    </form>
                    <form action="/hapus" method="post" style="display:inline;">
                        <input type="hidden" name="id" value="<%= result[i].id %>">
                        <button type="submit" class="btn btn-danger">Hapus</button>
                    </form>
                </li>
            <% } %>
        </ul>
    </div>
</body>
</html>
